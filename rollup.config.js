// rollup.config.js
import { defineConfig } from "rollup";
import image from "@rollup/plugin-image";

export default defineConfig({
  // Konfigurasi lainnya
  plugins: [
    // Plugin lainnya
    image(),
  ],
});
